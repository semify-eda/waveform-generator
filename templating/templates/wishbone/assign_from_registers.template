
{% for register in registers -%}
    {%- set outer_loop = loop -%}
    {%- for entry in registers[register].entries -%}
        {%- set current_entry = registers[register].entries[entry] -%}
        {{'{:<10} wbs_dat_o[{:>2}:{:>2}] = {};'.format(registers[register].address + ':', current_entry.MSB, current_entry.LSB, register.lower() + '_' + entry.lower() + '_ff')}}
{% endfor -%}
{%- endfor %}
